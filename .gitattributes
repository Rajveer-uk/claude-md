# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must stay LF so they run on Linux/macOS
*.sh text eol=lf

# PowerShell scripts stay CRLF (Windows)
*.ps1 text eol=crlf
